// 待完善，双指针扩散
var longestPalindrome = function(s) {
  const len = s.length
  const times = Math.floor(len / 2)
  const list = s.split('')
  let result = []
  if (len & 1) {
    // 奇数

  } else {
    // 偶数
  }
};

 console.log('longestPalindrome', longestPalindrome('abcdaaa')) 